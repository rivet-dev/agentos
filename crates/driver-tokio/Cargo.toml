[package]
name = "agentos-driver-tokio"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Process-owned agentOS Tokio driver and bounded blocking executor"

[dependencies]
agentos-resource-accounting = { workspace = true }
tokio = { version = "1", features = ["macros", "rt", "rt-multi-thread", "sync", "time", "net"] }
