[package]
name = "agentos-vm-config"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared agentOS VM creation configuration types"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ts-rs = { version = "10.1", features = ["import-esm", "no-serde-warnings", "serde-compat"] }
