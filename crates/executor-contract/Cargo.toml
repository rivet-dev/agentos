[package]
name = "agentos-executor-contract"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Runtime-neutral executor and host-capability contracts for agentOS"

[lib]
doctest = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_bytes = "0.11"
serde_json = "1"
