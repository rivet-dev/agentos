[package]
workspace = "../../../../../toolchain"
name = "cmd-tar"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "tar standalone binary for agentos VM"

[[bin]]
name = "tar"
path = "src/main.rs"

[dependencies]
agentos-tar = { path = "../tar" }
