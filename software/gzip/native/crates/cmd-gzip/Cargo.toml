[package]
workspace = "../../../../../toolchain"
name = "cmd-gzip"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "gzip standalone binary for agentos VM"

[[bin]]
name = "gzip"
path = "src/main.rs"

[dependencies]
agentos-gzip = { path = "../gzip" }
