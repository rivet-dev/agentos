[package]
workspace = "../../../../../toolchain"
name = "agentos-gzip"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "gzip/gunzip/zcat implementations for agentos standalone binaries"

[dependencies]
flate2 = { version = "1.0", default-features = false, features = ["rust_backend"] }
