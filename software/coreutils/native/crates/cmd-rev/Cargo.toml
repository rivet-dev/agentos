[package]
workspace = "../../../../../toolchain"
name = "cmd-rev"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "rev standalone binary for agentos VM"

[[bin]]
name = "rev"
path = "src/main.rs"

[dependencies]
agentos-rev = { path = "../rev" }
