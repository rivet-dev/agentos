[package]
workspace = "../../../../../toolchain"
name = "cmd-sleep"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "sleep standalone binary for agentos VM"

[[bin]]
name = "sleep"
path = "src/main.rs"

[dependencies]
agentos-builtins = { path = "../../../../../toolchain/crates/libs/builtins" }
