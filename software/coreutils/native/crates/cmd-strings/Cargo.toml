[package]
workspace = "../../../../../toolchain"
name = "cmd-strings"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "strings standalone binary for agentos VM"

[[bin]]
name = "strings"
path = "src/main.rs"

[dependencies]
agentos-strings-cmd = { path = "../strings-cmd" }
