[package]
workspace = "../../../../../toolchain"
name = "cmd-mkdir"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "mkdir standalone binary for agentos VM"

[[bin]]
name = "mkdir"
path = "src/main.rs"

[dependencies]
uu_mkdir = "0.7.0"
