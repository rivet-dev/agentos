[package]
workspace = "../../../../../toolchain"
name = "cmd-expr"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "expr standalone binary for agentos VM"

[[bin]]
name = "expr"
path = "src/main.rs"

[dependencies]
agentos-expr = { path = "../expr" }
