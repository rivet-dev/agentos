[package]
workspace = "../../../../../toolchain"
name = "cmd-awk"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "awk standalone binary for agentos VM"

[[bin]]
name = "awk"
path = "src/main.rs"

[dependencies]
agentos-awk = { path = "../awk" }
