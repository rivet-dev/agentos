[package]
workspace = "../../../../../toolchain"
name = "agentos-awk"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "awk implementation for agentos standalone binaries"

[dependencies]
awk-rs = "0.1.0"
