[package]
workspace = "../../../../../toolchain"
name = "cmd-diff"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "diff standalone binary for agentos VM"

[[bin]]
name = "diff"
path = "src/main.rs"

[dependencies]
agentos-diff = { path = "../diff" }
