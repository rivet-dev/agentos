[package]
name = "cmd-stubs"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "_stubs mini-multicall binary for unsupported commands in agentos VM"

[[bin]]
name = "_stubs"
path = "src/main.rs"

[dependencies]
agentos-stubs = { path = "../../libs/stubs" }
