[package]
name = "agentos-builtins"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Built-in command implementations (sleep, test/[, whoami) for agentos VM"

[dependencies]
libc = "0.2"
wasi-ext = { path = "../../wasi-ext" }
