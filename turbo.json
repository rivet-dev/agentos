{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"//#fmt": {
			"cache": false
		},
		"build": {
			"dependsOn": ["^build"],
			"inputs": [
				"src/**",
				"tsconfig.json",
				"package.json"
			],
			"outputs": ["dist/**"]
		},
		"check-types": {
			"inputs": [
				"src/**",
				"tests/**",
				"tsconfig.json",
				"package.json"
			]
		},
		"test": {
			"inputs": ["src/**", "tests/**", "package.json"],
			"dependsOn": ["^build", "check-types"]
		}
	}
}
