# AgentOS internal TypeScript compiler

Public AgentOS TypeScript companion package backed by AgentOS runtime primitives.

This private workspace module implements the compiler used by
`@rivet-dev/agentos-javascript`. It is not a supported install target. Use
`runtime.typescript` when you need isolated TypeScript type
checking or compilation on top of the maintained AgentOS runtime surface.
